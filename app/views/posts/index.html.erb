<h1>List of posts: <%= @posts.count %></h1>
<ul>
    <% @posts.each do |post| %>
    <li>
        <p>
            <%= link_to post.title, post_path(post) %> created by <%= post.user.login %>
        </p>
    </li>
    <span>
        Post has <%= pluralize post.comments.count, 'comment' %>
    </span>
    <% end %>
</ul>